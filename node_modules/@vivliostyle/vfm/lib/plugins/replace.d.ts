import { Node } from 'unist';
export interface ReplaceRule {
    test: RegExp;
    match: (result: RegExpMatchArray, h: any) => Node | string;
}
export declare function replace({ rules }?: {
    rules?: ReplaceRule[];
}): ((tree: Node) => any) | undefined;
