import upath from 'upath';
export const checkOutputFormat = (v) => {
    return ['pdf', 'webpub', 'epub'].includes(v);
};
export const checkRenderMode = (v) => {
    return ['local', 'docker'].includes(v);
};
export const checkPreflightMode = (v) => {
    return ['press-ready', 'press-ready-local'].includes(v);
};
export function detectOutputFormat(outputPath) {
    const lowerCasedExt = upath.extname(outputPath);
    if (lowerCasedExt === '.pdf') {
        return 'pdf';
    }
    else if (lowerCasedExt === '.epub') {
        return 'epub';
    }
    else {
        return 'webpub';
    }
}
//# sourceMappingURL=output-types.js.map