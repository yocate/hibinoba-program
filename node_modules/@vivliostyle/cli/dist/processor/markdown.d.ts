import { Metadata, StringifyMarkdownOptions } from '@vivliostyle/vfm';
import { VFile } from 'vfile';
import { DocumentProcessorFactory } from '../input/config.js';
export interface VSFile extends VFile {
    data: {
        title?: string;
        theme?: string;
    };
}
export declare function processMarkdown(documentProcessorFactory: DocumentProcessorFactory, filepath: string, options?: StringifyMarkdownOptions): Promise<VSFile>;
export declare function readMarkdownMetadata(filepath: string): Metadata;
//# sourceMappingURL=markdown.d.ts.map