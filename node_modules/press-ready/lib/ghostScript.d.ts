import shell from 'shelljs';
export interface GhostscriptOption {
    inputPath: string;
    outputPath: string;
    pdfxDefTemplatePath?: string;
    sourceIccProfilePath?: string;
    grayScale?: boolean;
    enforceOutline?: boolean;
    boundaryBoxes?: boolean;
    title?: string;
}
export declare function isGhostscriptAvailable(): shell.ShellString;
export declare function ghostScript({ inputPath, outputPath, pdfxDefTemplatePath, sourceIccProfilePath, grayScale, enforceOutline, boundaryBoxes, title, }: GhostscriptOption): Promise<{
    command: [string, string[]];
    rawOutput: any;
    rawError: any;
}>;
